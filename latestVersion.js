// This file is imported with a <script> tag through a CDN to allow CrypNote to check what the
// latest version is. We can't query GitHub directly through its API or anything because CORS
// disallows `file://` URLs from sending requests anywhere.
latestVersion = "0.1.0";
